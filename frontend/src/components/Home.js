const Home = (props) => {

    // -------- START HANDLERS ---------
	// -------- END HANDLERS -----------

	return (
		<div className="wrapper">
			<div className="container">
				Hi
			</div>
		</div>
	);
};

export default Home;
